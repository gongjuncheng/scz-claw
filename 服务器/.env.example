# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# DeepSeek（AI 邮件生成）
DEEPSEEK_API_KEY=
# 可选：自定义兼容接口地址 / 模型（留空则默认 DeepSeek）
# AI_BASE_URL=https://api.deepseek.com/v1
# AI_MODEL=deepseek-chat

# 加密密钥（AES-256，用于加密 TOTP 密钥，请设置 32 字节随机 hex）
# 生成命令： node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
